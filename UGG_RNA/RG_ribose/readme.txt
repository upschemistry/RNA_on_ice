All of the folders inside this folder contain pull runs (and related runs) that focus on the middle G ribose.

The step_180_to_0 folder contains the most recent pull results and was conducted over 20 ns, twice as long as the initial pull runs. These initial pull runs were split into two folders: step_to_0_1ns and step_to_180_1ns.